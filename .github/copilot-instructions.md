# General instructions

The python code in this repo runs inside a docker container which is defined in the ecvs.dockerfile

Any software that needs to be installed for dependencies etc should be added in the dockerfile and the docker image rebuilt before use.