# Readme

This repo relies oņ you having downloaded a untar a version of blender and accessing that blender progammatically and running your python scripts within it

```
cd ../$ROOT-of-this-repo
wget https://download.blender.org/release/Blender5.0/blender-5.0.1-linux-x64.tar.xz
tar -xf blender-5.0.1-linux-x64.tar.xz
```

The shell scripts then path into that directly
